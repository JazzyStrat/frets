## Q - Why only key mode seems to slip state do to requestAnimationFrame() callback's async callback
this is fixed now, but it would be nice to better understand the fix.


## TODO - requestAnimationFrame() RUNS faster on ios 120 60, rutro. Fix.
the general fix is to make things based on time and not cycles.. i think.

## TODO - left-handed mode
probably only need a toggle to trigger CSS Flex orientation

